import { Stack } from '@mantine/core';
import { getFullViewHeight } from './MobileLayout';
export const Home = () => {

  return (
    <Stack style={{ height: getFullViewHeight() }} className='relative'>

    </Stack>
  );
}; 
